const DEV = true

const ROT_SEED = 26

const TILESET = {
    PLAYER: 0,
    MONSTER: 1,
    WALL: 2,
    FLOOR: 3
}

const GAME_STATE = {
    CAN_PLAY: "Can play",
    WAIT: "Wait"
}

const TILE_SIZE = 16

const DIRECTIONS = {
    LEFT: "Left",
    RIGHT: "Right",
    TOP: "Top",
    BOTTOM: "Bottom"
}

export {
    DEV,
    DIRECTIONS,
    ROT_SEED,
    TILESET,
    GAME_STATE,
    TILE_SIZE
}